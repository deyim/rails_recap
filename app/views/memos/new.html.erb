<h1>new memo</h1>

<%= bootstrap_form_for(@memo) do |f| %>
<%# FIXME: current user's user ID to user_id field%>
  <%= f.hidden_field :user_id, value: 1 %>
  <%= f.text_field :title, label: '제목' %>
  <%= f.text_area :content, :class => "tinymce", :rows => 20, :cols => 80 %>
  <%= tinymce %>
  <%= f.submit "Create" %>
<% end %>

